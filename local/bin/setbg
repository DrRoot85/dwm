#!/bin/bash

# Pick an image using nsxiv
selected_image=$(nsxiv -r -q -o ~/Pictures/MyWalls/"$1")

# Extract colors with pywal
wal -i "$selected_image"  -o  $SCRIPTSDIR/reload_dunst

# Set Xwallpaper background
xwallpaper --stretch "$selected_image"
sleep 0.5
/home/DrRoot/.local/bin/reload_dunst